import React from "react";

const TransferForm = (props) => {
    let accountId = props.accountId;
    let accountBalance = props.balance;

    return (
    <div>
        
    </div>
    );
};

export default TransferForm;
